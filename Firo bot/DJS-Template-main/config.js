module.exports = {
    token: '', // Bot token.
    prefix: '', // Bot prefix.
    owners: [], // An array of user ids who are counted as bot owner.

    commandCooldown: 0 // Must be an integer (number), 0 if you don't want a cooldown.. 
}
